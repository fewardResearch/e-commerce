export default interface ISlider {
    id: number;
    title: string;
    url: string;
    bgDesktopImage?: string;
    bgMobileImage?: string;
    contentImage?: string;
    bgColor?: string;
    alt: string;
}